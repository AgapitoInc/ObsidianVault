<%*
const clientName = await tp.system.prompt("Client Name");
const address = await tp.system.prompt("Address (optional)", "");

// Rename and move file to Clients folder
await tp.file.move(`Clients/${clientName}`);
-%>
---
type: client
name: <% clientName %>
address: <% address %>
created: <% tp.date.now("YYYY-MM-DD") %>
status: active
---

# <% clientName %>

## Address
<% address %>

## Contacts

| Name | Role | Email | Phone |
|------|------|-------|-------|
| | Primary | | |
| | | | |

## Notes


## Projects

```button
name + New Project
type command
action QuickAdd: New Project
```

```dataview
TABLE
  project_number as "Project #",
  project_name as "Name",
  status as "Status",
  percent_complete + "%" as "Progress"
FROM "Projects"
WHERE type = "project" AND contains(client, this.file.name)
SORT project_number DESC
```
